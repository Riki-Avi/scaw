document.addEventListener('DOMContentLoaded', () => {
    console.log("Aplicación de resumen inicializada.");
    // Aquí podremos agregar lógica interactiva (ej. acordeones, filtros, cuestionarios) a medida que agreguemos el contenido.
});
